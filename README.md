# demo-cordova
cordova + angularjs + grunt

#### Instalar dependencias
```sh
$ npm install -g grunt-cli cordova
$ npm install
$ bower install
```

#### Generar
```sh
$ grunt build
```

#### Ejecutar
```sh
$ cordova platform add android
$ cordova run android
```